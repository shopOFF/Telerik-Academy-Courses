namespace _02.DefiningClasses
{
    using System;

    // Ако направим статичен клас, в него не може да съществува нищо инстанционно и не може и да се създават подобни неща !!!
    public static class Printer
    {
        // public static void PrintCat(Cat cat)  // правим си статичен метод който взима котка и ще я принтира
        //  {
        //      Console.WriteLine(cat.Name + " " + cat.Age);
        //  }

        // и по този начин си извикваме и изполваме статичния метод в класа(през самият клас)
        // Printer.PrintCat(yetAnotherCat);



    }
}
