// 01. Operators by categories in JavaScript
//Category	Operators
//Arithmetic	+ - * / % ++ --
//Logical	&& || ^ !
//Binary	& | ^ ~ << >> >>>
//Comparison	== != < > <= >= === !==
//Assignment	= += -= *= /= %= |= ^= <<= >>=
//Concatenation	+
//Other	
//.-да бъркаме в обекта,
//[]-индексатора, 
//()-да сменяме приоритета на операции и да викаме функии или въобще да викаме нещо
//?: - тернарен оператор
//new-когато се създават нови обекти
//in -проверява дали даден обект има дадено пропърти
// delete- трие пропъртита от обекти
// void-нищо полезно не прави
// typeof-казва типът на нещо
// instanceof-проверява дали нещо наследява друго нещо

// 02. Как да делиме целочислено число(да ни връща целочислено число), а не както обикновенно в джаваскрипт двойно
// 5 / 2 = 2.5 обикновенно
// 5 / 2 | 0 = 2  но така вече връща 2 (цяло число)

// 03.Comparison Operators
//Comparison operators are used to compare variables
//==, <, >, >=, <=, !=,===,!==
//For equality comparison, the use of === and !== is preferred  !!!!!!!!!!!!!
// let a = 5;
// let b = 4;

//console.log(a >= b);   // True
//console.log(a != b);   // True
//console.log(a == b);   // False
//console.log(0 == '');  // True
//console.log(0 === ''); // False